<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thoughtWorks.dao.SubjectDao">
    <insert id="addSubject" parameterType="Subject">
    INSERT INTO o_subject (subject,subject_img,option_a,option_b,
    option_c,option_d,correct,questions_id,chapter,facility,type,)
    VALUES (subject,subjectImg,optionA,optionB,optionC,optionD,
    correct,questionsId,chapter,facility,type,)
    </insert>

    <select id="selectQuestions" resultType="Questions">
        SELECT *
        FROM o_questions
    </select>
    <!--<update id="update" parameterType="Role">-->
    <!--UPDATE t_role-->
    <!--SET t_role.name = #{name}, t_role.available = #{available}-->
    <!--WHERE t_role.id = #{id}-->
    <!--</update>-->

</mapper>